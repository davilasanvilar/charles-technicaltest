import { BookingsTable } from '../BookingsTable';
import { Layout } from '../Layout';

export function BookingsScreen() {

    return (
        <Layout>
            <BookingsTable/>
        </Layout>
    )
}